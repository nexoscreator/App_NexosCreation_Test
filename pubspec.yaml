name: nexoscreation
description: "Official of Nexos creation."

publish_to: "none"

version: 0.1.0

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  google_fonts: ^2.3.2
  url_launcher: ^6.1.7
  webview_flutter: ^3.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/twitter.png
    - assets/facebook.png
    - assets/linkedin.png
    - assets/instagram.png
